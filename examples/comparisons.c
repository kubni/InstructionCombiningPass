#include <stdio.h>
#include <stdbool.h>

int main() {
    int y = 2;
    int z = 3;
    // if(y > 0) {
    //     z++;
    // } 

    // if(y < 0) {
    //     z--;
    // }

    // if(y <= 0) {
    //     z = 0;
    // }

    // if(y >= 0) {
    //     z = 3;
    // }

    bool a = true;
    bool b = false;

    if (a == b) {
        z =5;
    } 

    if (a != b) {
        z=5;
    }

    return 0;
}